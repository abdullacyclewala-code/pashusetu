import {NextResponse} from "next/server";import {createClient} from "@supabase/supabase-js";import {ingestTelemetry} from "@/lib/iot/ingest";
export async function POST(req:Request){try{const key=req.headers.get("x-device-key")??"";if(!key)return NextResponse.json({error:"x-device-key is required"},{status:401});const db=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!,{auth:{persistSession:false}});const result=await ingestTelemetry(db,await req.json(),key);return NextResponse.json(result,{status:result.duplicate?200:201});}catch(e){const x=e as Error&{status?:number};return NextResponse.json({error:x.message||"Telemetry ingest failed"},{status:x.status??400});}}
