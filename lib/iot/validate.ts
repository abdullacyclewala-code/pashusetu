import type {TelemetryInput} from "./types";
const ranges:Record<string,[number,number]>= {bodyTempC:[30,45],ruminationMin:[0,800],activity:[0,200],milkYieldKg:[0,80],envTempC:[-10,60],humidityPct:[0,100],ammoniaPpm:[0,200],batteryPct:[0,100]};
export function parseTelemetry(v:unknown):TelemetryInput{if(!v||typeof v!=="object")throw new Error("Body must be an object");const x=v as Record<string,unknown>;for(const k of ["externalId","ingestId","timestamp"])if(typeof x[k]!=="string"||!x[k])throw new Error(`${k} is required`);if(!/^[0-9a-f-]{36}$/i.test(x.ingestId as string))throw new Error("ingestId must be a UUID");if(!Number.isFinite(Date.parse(x.timestamp as string)))throw new Error("timestamp must be ISO-8601");for(const [k,[lo,hi]] of Object.entries(ranges))if(x[k]!=null&&(typeof x[k]!=="number"||!Number.isFinite(x[k])||(x[k] as number)<lo||(x[k] as number)>hi))throw new Error(`${k} must be between ${lo} and ${hi}`);return x as unknown as TelemetryInput;}
